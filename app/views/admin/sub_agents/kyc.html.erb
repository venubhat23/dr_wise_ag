<!-- Page Header -->
<div class="d-flex justify-content-between align-items-center mb-4">
  <div>
    <h2 class="mb-1"><%= @sub_agent.display_name.presence || 'Unnamed Affiliate' %> - Submit KYC</h2>
    <p class="text-muted mb-0">
      <% kyc_badge = { 'pending' => 'secondary', 'submitted' => 'warning text-dark', 'approved' => 'success', 'rejected' => 'danger' }[@sub_agent.kyc_status] %>
      <span class="badge bg-<%= kyc_badge %> me-2">KYC: <%= @sub_agent.kyc_status.capitalize %></span>
      Upload KYC documents on behalf of this affiliate, or mark KYC as done without any documents.
    </p>
  </div>
  <div class="d-flex gap-2">
    <%= link_to documents_admin_sub_agent_path(@sub_agent), class: "btn btn-outline-secondary" do %>
      <i class="bi bi-files me-1"></i>All Documents
    <% end %>
    <%= link_to admin_sub_agents_path, class: "btn btn-outline-secondary" do %>
      <i class="bi bi-arrow-left me-1"></i>Back to Affiliates
    <% end %>
  </div>
</div>

<%= form_with url: submit_kyc_admin_sub_agent_path(@sub_agent), method: :post, local: true, multipart: true do |f| %>
  <div class="card border-0 shadow-sm mb-4">
    <div class="card-header bg-transparent border-0">
      <h5 class="mb-0"><i class="bi bi-person-vcard me-2 text-primary"></i>KYC Details <span class="text-muted small fw-normal">(optional)</span></h5>
    </div>
    <div class="card-body">
      <div class="row g-3">
        <div class="col-md-6">
          <%= f.label :first_name, "First Name", class: "form-label" %>
          <%= f.text_field :first_name, value: @sub_agent.first_name, class: "form-control" %>
        </div>
        <div class="col-md-6">
          <%= f.label :last_name, "Last Name", class: "form-label" %>
          <%= f.text_field :last_name, value: @sub_agent.last_name, class: "form-control" %>
        </div>
        <div class="col-md-6">
          <%= f.label :aadhaar_no, "Aadhaar Number", class: "form-label" %>
          <%= f.text_field :aadhaar_no, value: @sub_agent.aadhaar_no, class: "form-control", placeholder: "12-digit Aadhaar number" %>
        </div>
        <div class="col-md-6">
          <%= f.label :pan_no, "PAN Number", class: "form-label" %>
          <%= f.text_field :pan_no, value: @sub_agent.pan_no, class: "form-control", placeholder: "e.g. ABCDE1234F" %>
        </div>
      </div>
    </div>
  </div>

  <div class="card border-0 shadow-sm mb-4">
    <div class="card-header bg-transparent border-0">
      <h5 class="mb-0"><i class="bi bi-cloud-upload me-2 text-success"></i>KYC Documents <span class="text-muted small fw-normal">(all optional)</span></h5>
    </div>
    <div class="card-body">
      <div class="row g-3">
        <% { aadhaar_file: ['Aadhaar Card', 'Aadhaar Card'], pan_file: ['Pancard', 'PAN Card'], bank_file: ['Bank Passbook', 'Bank Document'], photo_file: ['Profile Image', 'Profile Photo'] }.each do |field, (type, label)| %>
          <% existing = @documents.find { |d| d.document_type == type } %>
          <div class="col-md-6">
            <%= f.label field, label, class: "form-label" %>
            <%= f.file_field field, class: "form-control", accept: "image/*,application/pdf" %>
            <% if existing&.has_file? %>
              <small class="text-muted">
                Already uploaded <%= existing.created_at.strftime('%d %b %Y') %> -
                <%= link_to "view", existing.document_url, target: '_blank', rel: 'noopener' %>. Uploading again replaces it on screen.
              </small>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <div class="d-flex flex-wrap gap-2">
    <%= f.button "Save Documents", type: :submit, name: "kyc_action", value: "save", class: "btn btn-primary" %>
    <% unless @sub_agent.kyc_approved? %>
      <%= f.button type: :submit, name: "kyc_action", value: "approve", class: "btn btn-success",
                   data: { confirm: "Mark KYC as done for this affiliate? This approves the KYC and activates their account, even if no documents are uploaded.",
                           turbo_confirm: "Mark KYC as done for this affiliate? This approves the KYC and activates their account, even if no documents are uploaded." } do %>
        <i class="bi bi-check-circle me-1"></i>Save &amp; Mark KYC Done
      <% end %>
    <% end %>
  </div>
<% end %>
